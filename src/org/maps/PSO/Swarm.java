package org.maps.PSO;

import org.maps.GA.Chromosome;

import java.util.Vector;

import static org.maps.InputData.Constants.MAX_SWARM;

public class Swarm {
    Vector<Chromosome> S = new Vector<>(MAX_SWARM);
    public Swarm() {
        for(int i = 1; i<= MAX_SWARM; i++) {
            Chromosome c_rand;

        }
    }
}
